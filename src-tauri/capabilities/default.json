{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the dashboard window",
  "windows": ["dashboard"],
  "permissions": [
    "core:default",
    "opener:default",
    "autostart:default"
  ]
}
